Shared data store: git-ignored bytes + hash-pinned manifests. SARCOS GPML
`.mat` files (Sarcos owns the split via `data.py`) and RTE (MEF). Never
fetch a file listed in manifest.json without verifying its sha256 first.
