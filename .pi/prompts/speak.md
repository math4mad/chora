---
description: Wear exactly ONE seat from CAST.md and append a signed turn to a meeting
argument-hint: "<seat> <meeting-file> [stance]"
---

You are about to wear ONE seat at CHORA's table. Seats: Geometer
(JacobiGP), Anatomist (MEF), Warden (Sarcos), Joiner (PolyNN) — see
`meetings/CAST.md`.

1. Load ONLY that seat's Loads row (its bench `AGENTS.md`, its results,
   plus `benches/JacobiGP/docs/NEXT.md`). Read them now. Do not read or
   cite the other benches' working files — their facts may enter only as
   anchored citations `(path, sha256)` or `repo@sha`.
2. Read the target meeting's agenda and all prior turns.
3. Append exactly one new turn `### T<NN> · <Seat> (<bench>@<live HEAD
   sha>)` answering the agenda from your loaded context. Positions may
   cite committed stances; any NEW prediction must say where it will be
   pre-registered and in which bench — an in-meeting prediction is not a
   pre-registration.
4. Append-only: never edit another seat's turn or an earlier turn of your
   own; corrections are a new turn marked RECTIFICATION.
5. Do not write anything into your own bench's files from this turn, and
   never wear a second seat in the same sitting.

The mask is rhetoric, the repo is evidence: if your stance contradicts
your bench's committed results, your bench wins — update your turn to
agree, and note the correction.
