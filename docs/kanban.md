# CHORA — the exp/dev board

<!-- GENERATED by bin/kanban.py. Hand-edit docs/experiments.json (intent), never this file. -->

Generated 2026-09-13T01:28:36Z · chora@f77f669

Rows 31 · in the record and asking nothing: 5 · lanes where a lane may say FAILS and mean it · **8 rows need reconciliation** (their flags are printed in full below, because a board that hides its own red rows is the instrument that froze today)

Lanes are derived from bytes (see `bin/kanban.py`'s docstring for the rules); `claim` is what the hand believes, and a disagreement is a red row.

| lane | id | row | owner | bench | verdict | next / gate |
|---|---|---|---|---|---|---|
| closed | `exp6-h6c` | H6c — the same dial read from two ends: does the evidence path, shown  | Geometer | JacobiGP | FAILS | — |
| closed | `h9m-draft-and-instrument` | H9-M: the critical-window DRAFT (currency = gain over the same-k floor | Anatomist | MEF | — | gated by MEF's answer to Letter 024: adopt, amend, or refuse the H9-M draft — the canonical text and the frozen date belong to MEF's commit, not to Kairos's draft and not to this board |
| closed | `polynn-exp8` | PolyNN exp8 — P-shape holds (jacobi > hermite by +1.15/+1.75 pp beyond | Joiner | PolyNN | — | — |
| closed | `ws-d1-publisher` | D1 — the publisher must never write while a sequencer owns the branch, | chair | chora | — | — |
| closed | `ws-manifest-existence` | The four-clause pin check (existence, not presence) — running on every | chair | chora | — | — |
| **RECONCILE** announced | `e0-seed14` | E0 ladder, seed 14, machine B (pretrain 742.5 s + 49 min sweep) — the  | Horologist | MEF | — | resolve the pretrain.log pin: land the bytes, or amend the entry to say plainly that they live on one laptop (asked of B in Letter 021 §5). |
| **RECONCILE** announced | `h6cB-donor-bytes` | B's donor bytes: 20 init-dump cells + 5 per-seed α,β walks, carrying t | Joiner | PolyNN | — | fetch B's bench branches on A (ws-fetch-B-branches) so the authorship pointers of these bytes resolve here. |
| **RECONCILE** announced | `sarcos-step0` | Sarcos Step 0 — dose–response calibration (5 doses × 3 seeds × 3 arms, | Warden | Sarcos | — | a protocol decision, not a run: no dose qualifies on the current shifted task, so either the task block changes or H9-S is retired with its numbers. |
| scored | `e3-last-mile` | MEF stage19 KAIROS-E3 — last-mile diagnostic (S-absorb / S-gate / S-sc | Horologist | MEF | NOT ADJUDICATED: S-sched 2/3 (seeds 14,15), gray/partial 1/3 (seed 13); zero seeds fired S-absorb or S-gate | send the constant-lr design finding to Kairos: the arms ran lr 5e-4 flat, so a firing S-sched cannot mean 'it was only the schedule'. The channel result (no closed channel at k=100 in this rig) goes with it as data, not as a closed item. |
| **RECONCILE** landed | `exp6-h6a-sarcos-pilot` | H6a on the sanctioned cheap pilot — Sarcos from-scratch rank-constrain | Geometer | JacobiGP | — | the pilot answers the curve-source gate from the cheap side while the Anatomist looks for the MEF rank sweeps; its ordering guard (A2 in HEAD, one draw only) is the same shape H6c used, so the same disclosure discipline applies to it. |
| **RECONCILE** equipped | `polynn-exp9` | PolyNN exp9 — depth ablation: where ReLU degrades, and whether Jacobi  | Joiner | PolyNN | — | band re-derived after the ×100 unit bug was caught at calibration (PolyNN@0eb2930); zero epochs run on either machine. Either run it or retire it — paperwork that is never spent is costume. |
| equipped | `ws-f3-glass-blind` | F3 — the glass is blind by construction: status.sh reads A's tree, so  | chair | chora | — | cheapest fix that needs no server: have B publish its own status.json to a per-machine path in this repo and let the page merge the two. D1 stopped the freeze; it did not give the glass eyes. |
| **RECONCILE** equipped | `ws-kanban` | This board — the auditor seat in its engineering sense: intent in a re | chair | chora | — | render it on the hosted glass (docs/index.html reads docs/kanban.json — the page should show lane counts + the red rows, not a second truth), then let the room try to make it lie: the four failures of 2026-09-12 (pinned-without-bytes, tracked-without-pin, cited-without-commit, painted-into-limbo) are its seed cases. |
| equipped | `ws-writer-lock` | A shared clone is still one writer — advisory lock for the sequences g | chair | chora | — | Meeting 004 Q3: the room ratifies this as practice, or orders one clone per session and retires it. Advisory by design — it cannot stop a hand, only make being stopped visible. |
| registered | `exp6-curve-source` | The gate both H6a and H6b wait on: a named curve file (r ∈ {2,8,32,64} | Anatomist | MEF | — | Either the Anatomist NAMES bytes that exist (a branch this machine has not fetched counts — say which), or this becomes a measured budget: 4 ranks × 2 seeds on the Qwen2.5+RTE rig, priced in units the bench has already printed. UPDATE 18:5x: the Geometer opened the cheap side instead of waiting — exp6-h6a-sarcos-pilot, registered by A2 at JacobiGP@0a60707. The MEF row still needs its named bytes or its budget. |
| registered | `polynn-paperwork-untracked` | PolyNN's docs/ is git-ignored: its pre-registrations and letters are o | Joiner | PolyNN | — | the Joiner's own fix: the .gitignore says 'the letters and the pre-registration keep their docs/ home on main' and then ignores docs/* — the !docs/LETTERS / !docs/PREREG.md exceptions do not reach children of an ignored directory, and PREREG-exp9.md is not in the exception list at all. Re-land Letter 006/004 + both PREREGs on main (Letter 020's restore went to a branch, not to main) or accept that exp9's pre-registration cannot be dated by any reader. |
| registered | `ws-fetch-B-branches` | Provenance that resolves HERE: A has not fetched B's bench branches, s | chair | chora | — | fetch refs/heads/bench-B-* in every bench on A (PolyNN@a0deb68, PolyNN@2fcb158 and Sarcos@b22f1e9 are the ones this board names). Then the `by: repo@sha` field means the same thing on both machines. |
| **RECONCILE** registered | `ws-phantom-numbers` | Numbers with no bytes: 'E4 seed 1002 → held-out PPL 2.7808' and 'exp14 | chair | chora | — | either they are another hand's unrecorded runs — in which case they cross by hash or they do not exist, and E4's pre-registration must declare itself post-hoc — or they never happened, and this row is the tombstone that keeps them out of the arithmetic. Ask the human; do not assume. |
| registered | `ws-pretrain-log-pin` | The carried exemption: E0_seed14_pretrain.log is pinned (d9b7adee7597… | Horologist | MEF | — | the writer of the entry (B, E0 seed 14) either lands the log or amends the entry to say plainly what models/ says in the open — these bytes live on one laptop and are not in git — keeping the hash as the witness of what was seen. A will not touch B's entry (law 3). |
| blocked | `e0-gate6-twins` | Gate 6 — the twins: an A-vs-B pair of the SAME seed, i.e. the programm | Warden | MEF | — | OPEN, not passed: the only A-vs-B ladder pair so far is cross-seed, so no machine-effect number is quotable from it. |
| blocked | `e1-curriculum` | KAIROS-E1 — curriculum swap as the timing knob's confound | Horologist | MEF | — | day order; unrun. |
| blocked | `e2-replant` | KAIROS-E2 — replant: fertilize-and-replant the soil mid-ladder (and it | Horologist | MEF | — | day order places it after E4; unrun on both machines. |
| blocked | `e4-activation-ladder` | KAIROS-E4 — activation-swap ladder (arms S SiLU / L fixed-Legendre / J | Horologist | MEF | — | never run: no script in any repo, no bytes in any manifest, no pre-registration. Its first move is paperwork, not compute. |
| blocked | `e4-prereg` | E4's pre-registration — the text that must exist before any E4 number: | Horologist | MEF | — | write it, commit it, THEN the script. The house standard is E3's: script committed at 09:13:55, process began at 09:22:34. |
| blocked | `exp6-h6a` | H6a — learned (α,β) as an early-warning gauge of mis-set rank (between | Geometer | JacobiGP | — | the Sarcos pilot (exp6-h6a-sarcos-pilot) can score the separation on cheap bytes while the MEF rank sweeps are located; the registered claim is about MEF's rig, so the pilot is a pre-flight, not the check. |
| blocked | `exp6-h6b` | H6b — a frozen threshold rule g(α̂,β̂) that PRESCRIBES r within ±1 oct | Geometer | JacobiGP | — | thresholds written into the run script before any pair is scored; the 8-pair composition frozen in the same commit. |
| **RECONCILE** blocked | `h9-mef-text` | H9-M on MEF's rig, REGISTERED — the canonical text, the frozen date, t | Anatomist | MEF | — | MEF answers Letter 024: adopt, amend, or refuse Kairos@ae6be01. Adopting means committing the text with a frozen date; the staged instrument (chora/experiments/h9m_seed15_and_band.py) checks for six specific clauses and refuses --force, so 'adopted' must be words in MEF's docs, not a mood. |
| blocked | `h9s-floor` | H9-S — does adaptation beat the frozen base at the earliest checkpoint | Horologist | Kairos | — | upgraded by Letter 011 from 'unmeasurable' to 'never yet measured'; Step 0 then found no qualifying dose. The floor is not flat, it is unvisited. |
| blocked | `iso-spectrality` | Isospectrality audit — σ-spectra of mlp.down_proj against own scales ( | Anatomist | MEF | — | MEF is dirty on A (untracked outputs/stage19_kairos_e3/). Committing or clearing that directory is a one-command unblock, and it has been blocking a third seed of work all afternoon. |
| blocked | `r1-densify` | R1 — adaptive ladder densification: bisect where |Δgain/Δk| exceeds ba | Horologist | Kairos | — | cannot start before bands exist — the registered guard is 'refining a bandless curve is fitting to noise with extra steps'. |
| blocked | `ws-d2-manifest-split` | D2 — split the append-only results array per bench (one writer each);  | chair | chora | — | agenda item, with its cost already measured: 8 h 08 min of glass, one conflicted manifest, and a staged resolution that had already dropped 13 of 27 pins while remaining valid JSON with correct hashes. |

## Flags (every red row, in full)

**`exp6-h6a-sarcos-pilot`** — H6a on the sanctioned cheap pilot — Sarcos from-scratch rank-constraint curves, fit by the Exp-4 evidence path (imported, not reimplemented)
- CLAIM DRIFT: the hand says 'equipped', the bytes say 'landed'

**`h6cB-donor-bytes`** — B's donor bytes: 20 init-dump cells + 5 per-seed α,β walks, carrying the programme's first exact replication (max_abs_diff 0.0 across two laptops)
- provenance PolyNN@a0deb68 resolves in no ref fetched here — the bytes crossed, their authorship pointer did not (law 2 half-satisfied on this machine)
- provenance PolyNN@2fcb158 resolves in no ref fetched here — the bytes crossed, their authorship pointer did not (law 2 half-satisfied on this machine)

**`e0-seed14`** — E0 ladder, seed 14, machine B (pretrain 742.5 s + 49 min sweep) — the band that E4 and R1 both need
- no bytes on disk: artifacts/results/mef/E0_seed14_pretrain.log

**`sarcos-step0`** — Sarcos Step 0 — dose–response calibration (5 doses × 3 seeds × 3 arms, both architectures, deterministic CPU)
- provenance Sarcos@b22f1e9 resolves in no ref fetched here — the bytes crossed, their authorship pointer did not (law 2 half-satisfied on this machine)

**`polynn-exp9`** — PolyNN exp9 — depth ablation: where ReLU degrades, and whether Jacobi turns the tide
- registered text lacks the marker 'depth ablation' at HEAD
- CLAIM DRIFT: the hand says 'registered', the bytes say 'equipped'

**`ws-phantom-numbers`** — Numbers with no bytes: 'E4 seed 1002 → held-out PPL 2.7808' and 'exp14 H6c fit lands within band' appeared in a log at session start and resolve in no ref, no reflog and no object of any clone here
- no bytes on disk: artifacts/results/mef/stage19_e4/e4_verdict_seed1002.json

**`ws-kanban`** — This board — the auditor seat in its engineering sense: intent in a registry, lanes derived from bytes, repainted by the same beat as the glass
- CLAIM DRIFT: the hand says 'registered', the bytes say 'equipped'

**`h9-mef-text`** — H9-M on MEF's rig, REGISTERED — the canonical text, the frozen date, the band and the obituary, in MEF's own docs
- no pre-registration: this row is an intention, not a registered check

## Workspace audits (the checks that don't belong to one row)

- pins: 118 · no bytes: 1 · hash mismatch: 0 · untracked-not-by-design: 0
-   DANGLING PIN (a permission slip for bytes that exist nowhere): artifacts/results/mef/E0_seed14_pretrain.log
- pin-baseline (docs/pin-baseline-2026-09-12-eve.txt): 0 of 108 pairs missing
- REPO chora: 4 unreachable snapshot commit(s) d57b4cc a8287be 786f909 7b277fc — a beat's work in limbo
- glass: generated 0 min ago · paint f77f669, 0 commit(s) behind HEAD
- citations that name no commit in any clone here: 2
-   UNRESOLVED CITATION 2fcb158 — cited by 2026-09-12-B-JacobiGP-h6c-bytes-land.md, INDEX.md (a rebase rewrote it, or it came from a branch never fetched)
-   UNRESOLVED CITATION a0deb68 — cited by INDEX.md (a rebase rewrote it, or it came from a branch never fetched)

## Standing law this board enforces

- **Nothing crosses without a hash** (law 2): a row is `landed` only when its bytes exist, are pinned, and the hash of the bytes *equals* the pin.
- **Four-way, not three** (learned 2026-09-12): pin ∧ on-disk ∧ hash-equal ∧ **tracked in git**. Each clause has failed alone, in this order, on this day.
- **Single writer** (law 3): a letter counts as announced only when committed in its own bench *and* mirrored here *and* indexed — three files, three failure modes.
- **Negatives are first-class** (law 4): `scored` includes FAILS and NOT ADJUDICATED; nothing about a lane implies a claim survived.
- **The instrument reports on itself**: the baseline diff, the sequencer check, the orphan-snapshot count and the glass staleness are rows of this board too.

